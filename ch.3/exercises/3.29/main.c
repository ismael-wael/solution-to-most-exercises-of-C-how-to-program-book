#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("enter the size of the square");
    int side ;
    scanf("%d",side);
    while(side>0)
    {
        while(side>0)
        {
            printf("*");
        }
        puts("");
    }
    return 0;
}
